{
  "name": "@surfaice/analyze",
  "version": "0.1.0",
  "description": "Static analyzer for Next.js App Router — generates .surfaice.md manifests",
  "main": "./dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "dev": "tsc --watch",
    "prepublishOnly": "pnpm build"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/surfaiceai/surfaice",
    "directory": "packages/analyze"
  },
  "dependencies": {
    "@surfaice/format": "workspace:*",
    "glob": "^11.0.0",
    "ts-morph": "^24.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
